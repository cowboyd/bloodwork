<h1>Patients {{#link-to "patients.new" tagName="button" }}Add{{/link-to}}</h1>
<ul>
  {{#each model as |patient|}}
    <li>
      <img src="{{patient.avatar}}" height="25" width="25">
      {{patient.name}}
    </li>
  {{/each}}
</ul>
