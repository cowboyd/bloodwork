<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <th>#</th>
      <th>Date</th>
      <th>HgB Level</th>
    </tr>
  </thead>
  <tbody>
    {{#each model.measurements as |measurement index|}}
      <tr>
        <td>{{index}}</td>
        <td>{{moment-format measurement.date "MMM DD YYYY"}}</td>
        <td>{{measurement.amount}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
